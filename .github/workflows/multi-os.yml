name: Cross-Platform Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test-matrix:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest] # 3 İşletim Sistemi

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      # Skip tests: JavaFX GUI tests fail in headless CI across all platforms
      # Linux/Mac ve Windows arasındaki komut farkını yönetmek gerekmez, mvn heryerde aynıdır.
      run: mvn -B package --file pom.xml -DskipTests
